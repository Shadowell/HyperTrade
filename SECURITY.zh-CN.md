# 安全政策

## 支持的版本

当前支持的 HyperTrade 版本：

| 版本 | 支持状态          |
| ------- | ------------------ |
| 0.1.x   | :white_check_mark: |
| < 0.1   | :x:                |

---

## 报告漏洞

**请勿通过公开的 GitHub Issue 报告安全漏洞。**

### 报告流程

如果您发现 HyperTrade 中的安全漏洞，请私下报告：

1. **邮件**：直接联系仓库所有者（见仓库设置）
2. **包含内容**：
   - 漏洞描述
   - 重现步骤
   - 潜在影响
   - 建议修复（如有）

### 响应时间

- **初始响应**：48 小时内
- **状态更新**：5 个工作日内
- **修复时间**：取决于严重性（见下文）

### 严重级别

| 严重性 | 描述 | 修复时间 |
|----------|-------------|--------------|
| **严重** | 远程代码执行、数据泄露、未授权访问生产系统 | 24-48 小时 |
| **高** | 权限提升、身份验证绕过、重大数据暴露 | 3-7 天 |
| **中** | 信息泄露、DoS、有限的权限提升 | 14-30 天 |
| **低** | 轻微信息泄漏、不可利用的漏洞 | 30-60 天 |

---

## 安全措施

### 当前保护

**认证与授权**：
- 特权操作的基于会话的认证
- HttpOnly、SameSite cookies，生产环境中启用安全标志
- 写入操作和控制操作的仅管理员路由
- 时间限制的会话

**数据保护**：
- 密钥存储在环境变量中，永远不在代码中
- 数据库凭证按环境隔离
- API 密钥和令牌安全存储
- 日志或追踪事件中无敏感数据

**API 安全**：
- 所有端点的输入验证
- 通过 SQLAlchemy ORM 防止 SQL 注入
- 对允许的来源进行 CORS 限制
- 速率限制（计划中）

**BitPro 集成**：
- 仅通过稳定的 MCP/API 契约访问
- 永不绕过 BitPro 风险边界
- 实盘操作的只读诊断
- 操作前的显式预检检查

**执行安全**：
- Testnet 订单的批准门禁
- 工具执行前的风控策略强制执行
- 写入操作的幂等性密钥
- V1 阻止主网执行

**基础设施**：
- Docker 容器隔离
- 网络分段（Nginx 反向代理）
- 最小容器权限
- 定期安全更新

### 已知限制

**V1 范围**：
- 主网实盘订单执行被有意阻止
- API 端点上无速率限制（V2 计划）
- 单一管理员用户（计划多用户认证）
- 会话管理基础（无刷新令牌）

**开发环境**：
- SQLite 模式仅用于开发（生产推荐 PostgreSQL）
- 开发服务器无 HTTPS 运行（生产使用 Nginx TLS）
- 调试日志可能包含敏感信息（生产中禁用）

---

## 安全最佳实践

### 操作员

**密钥管理**：
- 永远不要将 `.env` 文件提交到版本控制
- 为管理员账户使用强大且唯一的密码
- 定期轮换 API 密钥
- 安全存储生产密钥（环境变量、密钥管理器）

**访问控制**：
- 限制对生产服务器的访问
- 使用 SSH 密钥而不是密码
- 启用防火墙规则以限制访问
- 定期监控访问日志

**部署**：
- 生产中使用 HTTPS（带 TLS 证书的 Nginx）
- 保持 Docker 镜像和系统包更新
- 审查部署日志中的异常
- 启用特权操作的审计日志

### 开发者

**代码安全**：
- 永远不要硬编码密钥或凭证
- 验证所有用户输入
- 使用参数化查询（避免字符串插值）
- 安全处理异常（不向用户暴露堆栈跟踪）
- 审查依赖项的已知漏洞

**测试**：
- 包含安全测试用例
- 测试认证和授权逻辑
- 验证输入验证
- 检查 SQL 注入和 XSS 漏洞

**依赖项**：
- 保持依赖项最新
- 审查 Python 和 npm 包的安全公告
- 使用锁文件（`uv.lock`、`pnpm-lock.yaml`）进行可重现构建
- 定期审计依赖项

---

## 漏洞披露政策

### 我们的承诺

- 我们认真对待所有安全报告
- 我们将在 48 小时内确认收到
- 我们将定期提供进度更新
- 我们将在修复部署后公开披露漏洞

### 公开披露

漏洞修复后：

1. **修复已部署**：安全补丁发布并部署到生产环境
2. **宽限期**：7 天供用户更新（如适用）
3. **公开披露**：漏洞详情发布在：
   - CHANGELOG.md
   - 安全公告（如果 GitHub 仓库是公开的）
   - 发布说明

### 认可

我们将认可负责任地披露漏洞的安全研究人员：
- 在发布说明中致谢
- 在安全公告中署名
- 可选的公开认可（如果研究人员同意）

---

## 安全检查清单

### 生产部署

- [ ] `.env` 文件具有受限权限（600）
- [ ] 数据库凭证强大且唯一
- [ ] Nginx 配置了 TLS 证书
- [ ] 防火墙规则限制对必要端口的访问
- [ ] Docker 容器以最小权限运行
- [ ] 密钥未提交到版本控制
- [ ] 管理员密码强大且非默认
- [ ] SSH 访问仅基于密钥
- [ ] 系统包是最新的
- [ ] 应用程序日志被监控

### 代码审查

- [ ] 无硬编码的密钥或凭证
- [ ] 所有端点的输入验证
- [ ] SQL 查询使用参数化语句
- [ ] 错误消息不暴露敏感信息
- [ ] 特权路由的认证检查
- [ ] CORS 设置是限制性的
- [ ] 依赖项是最新的
- [ ] 安全测试通过

---

## 事件响应

### 发生安全事件时

1. **遏制**：立即隔离受影响的系统
2. **评估**：确定范围和影响
3. **通知**：通知仓库所有者和受影响方
4. **修复**：应用修复和补丁
5. **审查**：事后分析和文档
6. **改进**：更新安全措施以防止再次发生

### 紧急联系

- **仓库所有者**：见仓库设置
- **生产服务器**：使用紧急密钥的 SSH 访问
- **数据库**：运行手册中的备份和恢复程序

---

## 安全资源

### 外部参考

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CWE/SANS Top 25](https://cwe.mitre.org/top25/)
- [Python 安全最佳实践](https://python.readthedocs.io/en/stable/library/security_warnings.html)
- [FastAPI 安全](https://fastapi.tiangolo.com/tutorial/security/)
- [Node.js 安全最佳实践](https://nodejs.org/en/docs/guides/security/)

### 内部文档

- [部署指南](docs/deployment.md)
- [事件响应运行手册](docs/runbooks/incident-response.md)
- [架构 - 安全](docs/architecture/)

---

## 本政策的更新

本安全政策可能会定期更新。检查提交历史以了解更改。

**最后更新**：2026-07-05

---

## 负责任的披露

我们感谢以下安全研究人员：
- 遵循负责任的披露实践
- 提供详细报告
- 允许合理的修复时间
- 不出于恶意目的利用漏洞

感谢您帮助保护 HyperTrade 及其用户的安全！🔒
